---
- hosts: all
  gather_facts: no
  tasks:
    - name: Start Odoo service
      ansible.builtin.systemd:
        name: "{{ service_name }}"
        state: started
        enabled: yes
      become: yes
