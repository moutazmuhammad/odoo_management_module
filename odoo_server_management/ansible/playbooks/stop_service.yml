---
- hosts: all
  gather_facts: no
  tasks:
    - name: Stop Odoo service
      ansible.builtin.systemd:
        name: "{{ service_name }}"
        state: stopped
        enabled: no
      become: yes
